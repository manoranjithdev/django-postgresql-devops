version: "3"
services:
  web:
    build: .
    container_name: django-app
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres
    container_name: postgres-db
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
